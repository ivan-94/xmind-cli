# This workflow is managed as the cargo-dist contract for GitHub issues #5 and #6.
# Source Manifest: https://github.com/ivan-94/xmind-cli/issues/5, parent PRD
# https://github.com/ivan-94/xmind-cli/issues/1,
# https://github.com/ivan-94/xmind-cli/issues/6,
# https://github.com/ivan-94/xmind-cli/issues/8, PLAN.md Phase 18,
# Cargo.toml [workspace.metadata.dist], docs/technical/release-policy.md,
# docs/installation.md, CHANGELOG.md.
name: Release

on:
  pull_request:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write

jobs:
  dist-plan:
    name: cargo-dist plan
    runs-on: ubuntu-latest
    outputs:
      val: ${{ steps.plan.outputs.manifest }}
      tag: ${{ steps.plan.outputs.tag }}
      releasing: ${{ steps.plan.outputs.releasing }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install cargo-dist
        shell: bash
        run: |
          set -euo pipefail
          export CARGO_DIST_NO_MODIFY_PATH=1
          export CARGO_DIST_PRINT_QUIET=1
          curl --proto '=https' --tlsv1.2 -LsSf \
            https://github.com/axodotdev/cargo-dist/releases/download/v0.31.0/cargo-dist-installer.sh | sh
          dist_bin="${CARGO_HOME:-$HOME/.cargo}/bin"
          path_entry="$dist_bin"
          if command -v cygpath >/dev/null 2>&1; then
            path_entry="$(cygpath -w "$dist_bin")"
          fi
          echo "$path_entry" >> "$GITHUB_PATH"
          "$dist_bin/dist" --version

      - name: Plan release
        id: plan
        run: dist plan --output-format=json

  dist-build:
    name: cargo-dist build ${{ matrix.target }}
    needs: dist-plan
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-14
            binary: ./target/aarch64-apple-darwin/dist/xmind
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
            binary: ./target/aarch64-unknown-linux-gnu/dist/xmind
          - target: x86_64-apple-darwin
            os: macos-15-intel
            binary: ./target/x86_64-apple-darwin/dist/xmind
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            binary: ./target/x86_64-unknown-linux-gnu/dist/xmind
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            binary: ./target/x86_64-pc-windows-msvc/dist/xmind.exe
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pinned Rust toolchain from rust-toolchain.toml
        run: rustup show

      - name: Install cargo-dist
        shell: bash
        run: |
          set -euo pipefail
          export CARGO_DIST_NO_MODIFY_PATH=1
          export CARGO_DIST_PRINT_QUIET=1
          curl --proto '=https' --tlsv1.2 -LsSf \
            https://github.com/axodotdev/cargo-dist/releases/download/v0.31.0/cargo-dist-installer.sh | sh
          dist_bin="${CARGO_HOME:-$HOME/.cargo}/bin"
          path_entry="$dist_bin"
          if command -v cygpath >/dev/null 2>&1; then
            path_entry="$(cygpath -w "$dist_bin")"
          fi
          echo "$path_entry" >> "$GITHUB_PATH"
          "$dist_bin/dist" --version

      - name: Build release artifacts
        run: dist build --target=${{ matrix.target }} --artifacts=local

      - name: Smoke release binary
        run: |
          ${{ matrix.binary }} --version
          ${{ matrix.binary }} tree tests/fixtures/xmind/minimal.xmind --json
          ${{ matrix.binary }} validate tests/fixtures/xmind/minimal.xmind --json

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-${{ matrix.target }}
          path: target/distrib/*
          if-no-files-found: error

  github-release:
    name: GitHub Release
    needs:
      - dist-plan
      - dist-build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: target/distrib
          merge-multiple: true

      - name: Generate aggregate SHA256SUMS
        shell: bash
        run: |
          set -euo pipefail
          cd target/distrib
          mapfile -t artifacts < <(find . -maxdepth 1 -type f ! -name 'SHA256SUMS' ! -name '*.sha256' -exec basename {} \; | sort)
          if [ "${#artifacts[@]}" -eq 0 ]; then
            echo "No release archives found for SHA256SUMS generation" >&2
            exit 1
          fi
          : > SHA256SUMS
          for artifact in "${artifacts[@]}"; do
            shasum -a 256 "$artifact" >> SHA256SUMS
          done
          cat SHA256SUMS

      - name: Extract release notes
        shell: bash
        run: bash .github/scripts/extract-release-notes.sh "${{ github.ref_name }}" CHANGELOG.md target/release-notes.md

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: target/distrib/*
          generate_release_notes: false
          body_path: target/release-notes.md

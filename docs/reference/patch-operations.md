# Patch Operations

## Source Manifest

- Conversation: XMind CLI product design discussion
- Scope: Per-operation semantics for declarative patch files
- Last updated: 2026-05-21

## Common Fields

| Field | Required | Meaning |
| --- | --- | --- |
| `op` | yes | Operation name |
| `node` | op-specific | Source or target topic selector |
| `parent` | op-specific | Parent topic selector |
| `to` | op-specific | Destination parent selector |
| `position` | no | Child order at destination |
| `tree` | op-specific | Topic tree input |
| `if_exists` | no | Duplicate handling |
| `match_by` | no | Matching strategy |

## assert_exists

```yaml
- op: assert_exists
  node: path:/Roadmap/Q2
```

Fails with `not_found` if the selector does not resolve to one topic.

## assert_not_exists

```yaml
- op: assert_not_exists
  node: path:/Roadmap/Q2/Deprecated
```

Fails with `patch_conflict` if the selector resolves to any topic.

## add

```yaml
- op: add
  parent: path:/Roadmap/Q2
  title: Payment
```

Creates one child topic.

## add_tree

```yaml
- op: add_tree
  parent: path:/Roadmap/Q2
  position: last
  if_exists: error
  tree:
    title: Payment
```

Creates a complete subtree under `parent`.

## set

```yaml
- op: set
  node: path:/Roadmap/Q2/Payment
  fields:
    title: Payments
    note: Payment scope
```

Updates explicit fields only.

## replace_tree

```yaml
- op: replace_tree
  node: path:/Roadmap/Q2/Payment
  tree:
    title: Payment
    children: []
```

Replaces the selected subtree with the provided tree. The selected topic path is removed and recreated unless the implementation can preserve the root id safely.

## merge_tree

```yaml
- op: merge_tree
  target: path:/Roadmap/Q2/Payment
  match_by: title_path
  tree:
    title: Payment
    children:
      - title: Checkout
```

Merges by relative identity. Existing matched topics are updated; missing topics are created; unmatched existing topics are preserved unless `prune: true` is set.

## delete and delete_tree

```yaml
- op: delete_tree
  node: path:/Roadmap/Q2/Deprecated
```

Deletes the selected topic and descendants. `delete` is an alias for `delete_tree` unless `children_only` or `promote_children` is set.

## move and move_tree

```yaml
- op: move_tree
  node: path:/Roadmap/Q2/Payment
  to: path:/Roadmap/Q3
  position: last
```

Moves the selected topic and descendants. Moving a topic into itself or its descendants is a `patch_conflict`.

## copy and clone_tree

```yaml
- op: clone_tree
  node: path:/Roadmap/Q2/Payment
  to: path:/Roadmap/Q3
```

Copies the selected subtree. New topic ids are generated by default.

## ensure_path

```yaml
- op: ensure_path
  path: /Roadmap/Q2/Payment
```

Creates missing path segments. Existing segments are reused.

## sort_children

```yaml
- op: sort_children
  node: path:/Roadmap/Q2
  by: title
  order: asc
```

Sorts direct children only unless `recursive: true` is set.

## set_tree_metadata

```yaml
- op: set_tree_metadata
  node: path:/Roadmap/Q2/Payment
  recursive: true
  add_labels: [MVP]
```

Applies metadata changes to a subtree.

## Conflict Rules

A patch fails before writing when:

- any selector is ambiguous,
- an operation references a topic deleted by an earlier operation unexpectedly,
- two operations create the same path with incompatible `if_exists`,
- a move would create a cycle,
- validation fails.

Failure output includes `operation_index`.

